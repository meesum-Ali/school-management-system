# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Zitadel Configuration
NEXT_PUBLIC_ZITADEL_ISSUER=http://localhost
NEXT_PUBLIC_ZITADEL_CLIENT_ID=your-zitadel-client-id

# App Configuration
NEXT_PUBLIC_APP_VERSION=1.0.0

# Server-to-server issuer used by Next server/edge (Docker internal)
# Defaults to http://zitadel:8080 inside containers. Override for non-Docker dev.
# ZITADEL_INTERNAL_ISSUER=http://zitadel:8080

# Server token exchange through nginx so Host header stays 'localhost'
# (matches ZITADEL_EXTERNALDOMAIN in docker-compose)
# ZITADEL_TOKEN_BASE=http://nginx
# ZITADEL_EXTERNAL_HOST=localhost
